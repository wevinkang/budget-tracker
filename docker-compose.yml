services:
  app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - /home/wev/budget-data:/data
      - /home/wev/budget-imports:/imports
    env_file:
      - .env
    restart: unless-stopped
